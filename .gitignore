node_modules
dist
dist-ssr
.env
.env.local
*.local
.DS_Store
backups/
.vercel
.env*
!.env.example

# Old nested SLearn/ clone with unrelated real git history -- never
# meant to be tracked from the top-level repo (see git history notes).
SLearn/

# TypeScript incremental build cache -- machine-local, regenerated on
# every build, was accidentally tracked and kept showing up as "modified"
# after every single build.
*.tsbuildinfo

# Local Lighthouse CI run state -- lighthouserc.json's own report output
# goes to ./lighthouse-reports; this directory is lhci's internal scratch
# state and was committed by accident in the initial commit.
.lighthouseci/
.gstack/

# Playwright/Vitest local run output
playwright-report/
test-results/
coverage/

# Supabase CLI's local scratch/cache dir (version checks, pooler URL,
# project ref) -- machine-local, regenerated on every CLI invocation,
# was accidentally tracked in the initial commit and kept showing up
# as "modified" after every single supabase command.
supabase/.temp/
